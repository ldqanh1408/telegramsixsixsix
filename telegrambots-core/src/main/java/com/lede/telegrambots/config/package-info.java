/**
 * Application configuration properties ({@code AppProperties}). Depends on nothing.
 */
@org.springframework.modulith.ApplicationModule(
        displayName = "Config",
        allowedDependencies = {})
package com.lede.telegrambots.config;
